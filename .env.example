# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/jfk_documents?schema=public"

# Local Documents Configuration
# Set this to the path of your analysis output directory
# Example: /Users/yourname/documents/analysis-output
LOCAL_DOCUMENTS_PATH=/path/to/your/output/directory

# Media source: 'local' uses LOCAL_DOCUMENTS_PATH, 'remote' uses NEXT_PUBLIC_API_URL
# When set to 'local', the app will serve images and analysis from the local directory
# Note: Uses NEXT_PUBLIC_ prefix to be available client-side
NEXT_PUBLIC_MEDIA_SOURCE=local

# External API URL (used when MEDIA_SOURCE=remote)
NEXT_PUBLIC_API_URL=https://api.oip.onl

# =====================================================
# Usage Instructions
# =====================================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update the values:
#    - DATABASE_URL: Your PostgreSQL connection string
#    - LOCAL_DOCUMENTS_PATH: Path to your analysis output directory
#    - MEDIA_SOURCE: 'local' for local files, 'remote' for external API
#
# 3. Import documents from local directory:
#    npx ts-node scripts/importLocalDocuments.ts --path /your/output/path --collection "my-collection"
#
# 4. Start the development server:
#    npm run dev
#
# Expected directory structure for LOCAL_DOCUMENTS_PATH:
#   output/
#   ├── images/{document-hash}/
#   │   ├── page-01.png
#   │   ├── page-02.png
#   │   └── ...
#   └── analysis/
#       ├── document-manifest.json
#       ├── {document-hash}/
#       │   ├── page-1.json
#       │   ├── page-2.json
#       │   └── ...
#       ├── {document-hash}-metadata.json
#       └── {document-hash}-fulltext.txt
