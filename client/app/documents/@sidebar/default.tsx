import JFKSidebarPage from './page';

// Default export for the parallel route slot
// This ensures the sidebar is always rendered, even when navigating to nested routes
export default JFKSidebarPage;
