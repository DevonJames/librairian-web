# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/jfk_documents?schema=public"

# Media source: 'local' uses the Document Analysis Tool, 'remote' uses NEXT_PUBLIC_API_URL
# - local: Proxies requests through /api/analyzer-proxy to the Document Analysis Tool
# - remote: Uses the external API (NEXT_PUBLIC_API_URL)
NEXT_PUBLIC_MEDIA_SOURCE=local

# External API URL (used when NEXT_PUBLIC_MEDIA_SOURCE=remote)
NEXT_PUBLIC_API_URL=https://api.oip.onl

# Document Analysis Tool API URL
# The URL where the document-analysis-tool server is running (default: http://localhost:3001)
# This is used for document processing, media serving, and analysis
DOCUMENT_ANALYZER_URL=http://localhost:3001

# =====================================================
# AI Audio Generation (Optional)
# =====================================================
# These keys are required for generating podcasts and investigative reports

# xAI (Grok) API Key - Used for text generation
# Get your API key at: https://console.x.ai/
XAI_API_KEY=your_xai_api_key_here

# ElevenLabs API Key - Used for text-to-speech
# Get your API key at: https://elevenlabs.io/
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# =====================================================
# Usage Instructions
# =====================================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update the values:
#    - DATABASE_URL: Your PostgreSQL connection string
#    - DOCUMENT_ANALYZER_URL: URL of the Document Analysis Tool server
#    - NEXT_PUBLIC_MEDIA_SOURCE: 'local' for Document Analysis Tool, 'remote' for external API
#    - XAI_API_KEY: (Optional) For AI-powered podcast/report generation
#    - ELEVENLABS_API_KEY: (Optional) For text-to-speech in podcasts/reports
#
# 3. Start the Document Analysis Tool server (required for local mode):
#    cd /path/to/document-analysis-tool && npm start
#
# 4. Start the development server:
#    npm run dev
